# Entornos virtuales
arduino_env/
venv/
env/

#claude
.claude/
CLAUDE.md

# Basura de Python y cachés
__pycache__/
*.pyc
.vscode/

# Basura de compilación de Arduino
*.elf
*.bin
*.hex

# Fotos capturadas por vision_model/capture.py (dataset local, no versionado)
vision_model/images/

# Copia local de referencia del unit file de systemd instalado en /etc/systemd/system/ —
# lleva URL_APPSCRIPT (secreto) en texto plano en Environment=, nunca debe commitearse.
/systemd/urb_agric_system.service
